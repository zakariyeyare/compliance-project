# Dedicated variables for the Vitest environment to avoid touching production secrets
EMAIL_SERVER_PORT=4400
EMAIL_HOST=localhost
EMAIL_PORT=1025
EMAIL_USER=test_user
EMAIL_PASS=test_password
EMAIL_FROM="Compliance App Test <tester@example.com>"
EMAIL_ALLOWED_ORIGINS=http://localhost:4173
VITE_EMAIL_API_BASE=/api-test
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=test-anon-key
